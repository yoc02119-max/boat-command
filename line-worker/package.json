{
  "name": "boat-command-line-webhook",
  "private": true,
  "version": "1.0.1",
  "scripts": {
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "^4.72.0"
  }
}
