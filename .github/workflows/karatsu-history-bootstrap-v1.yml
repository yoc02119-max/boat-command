name: Karatsu history bootstrap 180d v1

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/karatsu-history-bootstrap-v1.yml'
      - 'program-history-builder-v121.py'

permissions:
  contents: write

concurrency:
  group: karatsu-history-bootstrap-v1
  cancel-in-progress: false

jobs:
  block:
    strategy:
      fail-fast: false
      matrix:
        block: [0, 1, 2]
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v6
      - run: sudo apt-get update -qq && sudo apt-get install -y -qq lhasa
      - name: Build 60-day Karatsu history block
        env:
          BLOCK: ${{ matrix.block }}
        run: |
          set -euo pipefail
          rm -rf karatsu-hist-work karatsu-hist-out
          mkdir -p karatsu-hist-work karatsu-hist-out
          python3 program-history-builder-v121.py --self-test

          python3 - <<'PY' > karatsu-hist-work/dates.txt
          import datetime,os
          from zoneinfo import ZoneInfo
          karatsuy=datetime.datetime.now(ZoneInfo('Asia/Tokyo')).date()
          start=karatsuy-datetime.timedelta(days=180)
          block=int(os.environ['BLOCK'])
          a=start+datetime.timedelta(days=block*60)
          b=min(a+datetime.timedelta(days=60),karatsuy)
          d=a
          while d<b:
              print(d.isoformat());d+=datetime.timedelta(days=1)
          PY

          : > karatsu-hist-work/exceptions.txt
          mkdir -p karatsu-hist-work/days

          while IFS= read -r d; do
            ymd="${d//-/}"; yyyy="${ymd:0:4}"; mm="${ymd:4:2}"; yy="${ymd:2:2}"; dd="${ymd:6:2}"; ym="${yyyy}${mm}"
            b="b${yy}${mm}${dd}.lzh"; k="k${yy}${mm}${dd}.lzh"
            rm -rf karatsu-hist-work/one
            mkdir -p karatsu-hist-work/one/raw karatsu-hist-work/one/b karatsu-hist-work/one/k karatsu-hist-work/one/out

            bc="$(curl -sS -L --connect-timeout 20 --max-time 60 -o karatsu-hist-work/one/raw/$b -w '%{http_code}' https://www1.mbrace.or.jp/od2/B/${ym}/$b || true)"
            sleep 5
            kc="$(curl -sS -L --connect-timeout 20 --max-time 60 -o karatsu-hist-work/one/raw/$k -w '%{http_code}' https://www1.mbrace.or.jp/od2/K/${ym}/$k || true)"
            sleep 5
            if [[ "$bc" != "200" || "$kc" != "200" ]]; then echo "$d HTTP B=$bc K=$kc" >> karatsu-hist-work/exceptions.txt; continue; fi

            lha xw=karatsu-hist-work/one/b karatsu-hist-work/one/raw/$b >/dev/null 2>&1 || { echo "$d B_EXTRACT_FAIL" >> karatsu-hist-work/exceptions.txt; continue; }
            lha xw=karatsu-hist-work/one/k karatsu-hist-work/one/raw/$k >/dev/null 2>&1 || { echo "$d K_EXTRACT_FAIL" >> karatsu-hist-work/exceptions.txt; continue; }

            python3 program-history-builder-v121.py --b-dir karatsu-hist-work/one/b --k-dir karatsu-hist-work/one/k --out karatsu-hist-work/one/out --venue 23 >/dev/null 2>&1 || { echo "$d BUILDER_FAIL" >> karatsu-hist-work/exceptions.txt; continue; }

            python3 - "$d" <<'PY'
          import json,sys
          from pathlib import Path
          d=sys.argv[1]
          rows=[]
          for p in Path('karatsu-hist-work/one/out/23').glob('*.json'):
              x=json.loads(p.read_text(encoding='utf-8'))
              rows += [r for r in x.get('races',[]) if r.get('d')==d]
          if not rows:
              raise SystemExit(0)
          ids=[r.get('id') for r in rows]
          if len(ids)!=len(set(ids)): raise SystemExit('DUPLICATE_DAY_IDS')
          if any(str(r.get('id','')).split('-')[3]!='23' for r in rows): raise SystemExit('WRONG_VENUE')
          if any(str(r.get('o','')).count('-')!=2 for r in rows): raise SystemExit('INVALID_RESULT')
          Path(f'karatsu-hist-work/days/{d}.json').write_text(json.dumps(rows,ensure_ascii=False,separators=(',',':')),encoding='utf-8')
          PY
          done < karatsu-hist-work/dates.txt

          python3 - <<'PY'
          import json,os
          from pathlib import Path
          rows=[]
          for p in sorted(Path('karatsu-hist-work/days').glob('*.json')):
              rows += json.loads(p.read_text(encoding='utf-8'))
          report={'block':int(os.environ['BLOCK']),'daysWithRaces':len({r['d'] for r in rows}),'races':len(rows),'exceptions':[x for x in Path('karatsu-hist-work/exceptions.txt').read_text().splitlines() if x.strip()]}
          Path('karatsu-hist-out/segment.json').write_text(json.dumps({'races':rows},ensure_ascii=False,separators=(',',':')),encoding='utf-8')
          Path('karatsu-hist-out/report.json').write_text(json.dumps(report,ensure_ascii=False,indent=2),encoding='utf-8')
          print(report)
          PY

      - uses: actions/upload-artifact@v4
        with:
          name: karatsu-history-block-${{ matrix.block }}
          path: karatsu-hist-out/*.json
          retention-days: 14

  merge:
    needs: block
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/download-artifact@v5
        with:
          pattern: karatsu-history-block-*
          path: karatsu-history-blocks
      - name: Merge and audit Karatsu history
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import datetime,json
          from collections import Counter
          from pathlib import Path
          from zoneinfo import ZoneInfo
          rows=[];reports=[]
          for p in Path('karatsu-history-blocks').rglob('segment.json'): rows+=json.loads(p.read_text(encoding='utf-8')).get('races',[])
          for p in Path('karatsu-history-blocks').rglob('report.json'): reports.append(json.loads(p.read_text(encoding='utf-8')))
          rows.sort(key=lambda r:(r['d'],int(r['r'])))
          ids=[r['id'] for r in rows]
          assert len(ids)==len(set(ids))
          assert all(str(r.get('id','')).split('-')[3]=='23' for r in rows)
          assert all(len(r.get('c',[]))==6 for r in rows)
          assert all(str(r.get('o','')).count('-')==2 for r in rows)
          by=Counter(r['d'] for r in rows)
          cutoff=(datetime.datetime.now(ZoneInfo('Asia/Tokyo')).date()-datetime.timedelta(days=1)).isoformat()
          partial={d:n for d,n in by.items() if n!=12}
          out={'schema':'boat-command-program-history-v2','builder':'KARATSU-HISTORY-BOOTSTRAP-V1','venueCode':'23','cutoff':cutoff,'resultUse':'HISTORICAL_ROWS_ONLY_STRICTLY_BEFORE_TARGET_DATE','exhibitionIncluded':False,'waterFeaturesIncluded':False,'races':rows}
          audit={'schema':'boat-command-karatsu-history-audit-v1','venueCode':'23','windowDays':180,'cutoff':cutoff,'races':len(rows),'raceDays':len(by),'complete12RaceDays':sum(1 for n in by.values() if n==12),'partialOrCancelledDays':len(partial),'partialRaceCounts':partial,'exceptions':sum((r.get('exceptions',[]) for r in reports),[]),'duplicateIds':len(ids)-len(set(ids)),'readyForResearch':len(rows)>=300,'readyForProductionPrediction':False}
          Path('karatsu-history-bootstrap-v1.json').write_text(json.dumps(out,ensure_ascii=False,separators=(',',':')),encoding='utf-8')
          Path('karatsu-history-audit-v1.json').write_text(json.dumps(audit,ensure_ascii=False,indent=2),encoding='utf-8')
          print(json.dumps(audit,ensure_ascii=False,indent=2))
          if len(rows)<300: raise SystemExit('KARATSU_HISTORY_MIN_300_NOT_REACHED')
          PY

      - name: Commit audited Karatsu history
        run: |
          set -euo pipefail
          git config user.name 'boat-command-karatsu-history'
          git config user.email 'boat-command-karatsu-history@users.noreply.github.com'
          git add karatsu-history-bootstrap-v1.json karatsu-history-audit-v1.json
          git diff --cached --quiet && { echo 'KARATSU_HISTORY_ALREADY_CURRENT'; exit 0; }
          git commit -m 'data(karatsu): publish audited 180-day history bootstrap'
          cp karatsu-history-bootstrap-v1.json /tmp/karatsu-history-bootstrap-v1.json
          cp karatsu-history-audit-v1.json /tmp/karatsu-history-audit-v1.json
          for attempt in 1 2 3 4 5; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/karatsu-history-bootstrap-v1.json karatsu-history-bootstrap-v1.json
            cp /tmp/karatsu-history-audit-v1.json karatsu-history-audit-v1.json
            git add karatsu-history-bootstrap-v1.json karatsu-history-audit-v1.json
            git diff --cached --quiet && exit 0
            git commit -m 'data(karatsu): publish audited 180-day history bootstrap'
            git push origin HEAD:main && exit 0
            sleep $((attempt*2))
          done
          exit 1
