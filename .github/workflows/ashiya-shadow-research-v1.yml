name: Ashiya SHADOW research v1

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Ashiya history bootstrap 180d v1", "Ashiya baseline backtest v1", "Ashiya 12R program snapshot v1"]
    types: [completed]
  schedule:
    - cron: '*/5 0-8,22-23 * * *'
  push:
    branches: [main]
    paths:
      - '.github/workflows/ashiya-shadow-research-v1.yml'
      - 'scripts/ashiya-shadow-research-v1.js'
      - 'ashiya-research-model-v1.js'

permissions:
  contents: write

concurrency:
  group: ashiya-shadow-research-v1
  cancel-in-progress: false

jobs:
  snapshot:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Refresh latest main
        run: |
          set -euo pipefail
          git fetch origin main
          git reset --hard origin/main
      - name: Resolve JST date
        id: date
        run: echo "date=$(TZ=Asia/Tokyo date +%F)" >> "$GITHUB_OUTPUT"
      - name: Create immutable Ashiya research snapshots
        run: |
          set -euo pipefail
          node --check ashiya-research-model-v1.js
          node --check scripts/ashiya-shadow-research-v1.js
          node scripts/ashiya-shadow-research-v1.js "${{ steps.date.outputs.date }}"
      - name: Audit research boundary
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import datetime,glob,json,re
          from zoneinfo import ZoneInfo
          JST=ZoneInfo('Asia/Tokyo')
          paths=glob.glob('live/ashiya/*/shadow/class-baseline/race-*.json')+glob.glob('live/ashiya/*/shadow/program-only/race-*.json')
          for p in paths:
              x=json.load(open(p,encoding='utf-8'))
              assert x['schema']=='boat-command-ashiya-shadow-research-v1'
              assert x['venue']=='ASHIYA' and x['venueCode']=='21'
              assert x['mode'] in {'CLASS_BASELINE','PROGRAM_ONLY'}
              if '/class-baseline/' in p: assert x['mode']=='CLASS_BASELINE'
              if '/program-only/' in p: assert x['mode']=='PROGRAM_ONLY'
              assert x['resultInput'] is False and x['payoutInput'] is False
              assert x['researchOnly'] is True and x['productionEnabled'] is False and x['tryEnabled'] is False
              assert x['cashNeutral'] is True and x['immutableAfterFirstWrite'] is True
              assert len(x['picks'])==4 and len(set(x['picks']))==4
              assert all(re.fullmatch(r'[1-6]-[1-6]-[1-6]',v) for v in x['picks'])
              assert abs(float(x['probabilitySum'])-1)<1e-9
              d=datetime.date.fromisoformat(x['date']);hh,mm=map(int,str(x['deadline']).split(':'))
              deadline=datetime.datetime(d.year,d.month,d.day,hh,mm,tzinfo=JST)
              generated=datetime.datetime.fromisoformat(str(x['generatedAt']).replace('Z','+00:00')).astimezone(JST)
              assert (deadline-generated).total_seconds()/60>=3
          print('ASHIYA_SHADOW_BOUNDARY_PASS')
          PY
      - name: Publish only new immutable shadow files
        run: |
          set -euo pipefail
          DATE="${{ steps.date.outputs.date }}"
          SHADOW="live/ashiya/$DATE/shadow"
          [ -d "$SHADOW" ] || { echo 'NO_ASHIYA_SHADOW_FILES'; exit 0; }
          rm -rf /tmp/ashiya-shadow-publish
          mkdir -p /tmp/ashiya-shadow-publish
          cp -a "$SHADOW/." /tmp/ashiya-shadow-publish/
          git config user.name 'boat-command-ashiya-shadow'
          git config user.email 'boat-command-ashiya-shadow@users.noreply.github.com'
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            mkdir -p "$SHADOW"
            while IFS= read -r src; do
              rel="${src#/tmp/ashiya-shadow-publish/}"; dst="$SHADOW/$rel"
              [ -f "$dst" ] && continue
              mkdir -p "$(dirname "$dst")"; cp "$src" "$dst"
            done < <(find /tmp/ashiya-shadow-publish -type f -name '*.json' | sort)
            git add "$SHADOW"
            git diff --cached --quiet && { echo 'ASHIYA_SHADOW_CURRENT'; exit 0; }
            git commit -m 'shadow(ashiya): freeze paired result-free research snapshots'
            git push origin HEAD:main && exit 0
            sleep 3
          done
          exit 1
