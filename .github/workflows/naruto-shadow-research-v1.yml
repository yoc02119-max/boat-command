name: Naruto SHADOW research v1

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Naruto history bootstrap 180d v1", "Naruto baseline backtest v1", "Naruto 12R program snapshot v1"]
    types: [completed]
  schedule:
    - cron: '*/5 0-8,22-23 * * *'
  push:
    branches: [main]
    paths:
      - '.github/workflows/naruto-shadow-research-v1.yml'
      - 'scripts/naruto-shadow-research-v1.js'
      - 'naruto-research-model-v1.js'

permissions:
  contents: write

concurrency:
  group: naruto-shadow-research-v1
  cancel-in-progress: false

jobs:
  snapshot:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Refresh latest main
        run: |
          set -euo pipefail
          git fetch origin main
          git reset --hard origin/main
      - name: Resolve JST date
        id: date
        run: echo "date=$(TZ=Asia/Tokyo date +%F)" >> "$GITHUB_OUTPUT"
      - name: Create immutable Naruto research snapshots
        run: |
          set -euo pipefail
          node --check naruto-research-model-v1.js
          node --check scripts/naruto-shadow-research-v1.js
          node scripts/naruto-shadow-research-v1.js "${{ steps.date.outputs.date }}"
      - name: Audit research boundary
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import datetime,glob,json,re
          from zoneinfo import ZoneInfo
          JST=ZoneInfo('Asia/Tokyo')
          paths=glob.glob('live/naruto/*/shadow/class-baseline/race-*.json')+glob.glob('live/naruto/*/shadow/program-only/race-*.json')
          for p in paths:
              x=json.load(open(p,encoding='utf-8'))
              assert x['schema']=='boat-command-naruto-shadow-research-v1'
              assert x['venue']=='NARUTO' and x['venueCode']=='14'
              assert x['mode'] in {'CLASS_BASELINE','PROGRAM_ONLY'}
              if '/class-baseline/' in p: assert x['mode']=='CLASS_BASELINE'
              if '/program-only/' in p: assert x['mode']=='PROGRAM_ONLY'
              assert x['resultInput'] is False and x['payoutInput'] is False
              assert x['researchOnly'] is True and x['productionEnabled'] is False and x['tryEnabled'] is False
              assert x['cashNeutral'] is True and x['immutableAfterFirstWrite'] is True
              assert len(x['picks'])==4 and len(set(x['picks']))==4
              assert all(re.fullmatch(r'[1-6]-[1-6]-[1-6]',v) for v in x['picks'])
              assert abs(float(x['probabilitySum'])-1)<1e-9
              d=datetime.date.fromisoformat(x['date']);hh,mm=map(int,str(x['deadline']).split(':'))
              deadline=datetime.datetime(d.year,d.month,d.day,hh,mm,tzinfo=JST)
              generated=datetime.datetime.fromisoformat(str(x['generatedAt']).replace('Z','+00:00')).astimezone(JST)
              assert (deadline-generated).total_seconds()/60>=3
          print('NARUTO_SHADOW_BOUNDARY_PASS')
          PY
      - name: Publish only new immutable shadow files
        run: |
          set -euo pipefail
          DATE="${{ steps.date.outputs.date }}"
          SHADOW="live/naruto/$DATE/shadow"
          [ -d "$SHADOW" ] || { echo 'NO_NARUTO_SHADOW_FILES'; exit 0; }
          rm -rf /tmp/naruto-shadow-publish
          mkdir -p /tmp/naruto-shadow-publish
          cp -a "$SHADOW/." /tmp/naruto-shadow-publish/
          git config user.name 'boat-command-naruto-shadow'
          git config user.email 'boat-command-naruto-shadow@users.noreply.github.com'
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            mkdir -p "$SHADOW"
            while IFS= read -r src; do
              rel="${src#/tmp/naruto-shadow-publish/}"; dst="$SHADOW/$rel"
              [ -f "$dst" ] && continue
              mkdir -p "$(dirname "$dst")"; cp "$src" "$dst"
            done < <(find /tmp/naruto-shadow-publish -type f -name '*.json' | sort)
            git add "$SHADOW"
            git diff --cached --quiet && { echo 'NARUTO_SHADOW_CURRENT'; exit 0; }
            git commit -m 'shadow(naruto): freeze paired result-free research snapshots'
            git push origin HEAD:main && exit 0
            sleep 3
          done
          exit 1
