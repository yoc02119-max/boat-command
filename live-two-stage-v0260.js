// BOAT COMMAND GAMAGORI TWO-STAGE PREDICTION v0.26.4
// FIRST = historical/program-composition only. Same-day results + exhibition excluded.
// SECOND = verified LIVE candidate. No result fetch here.
(()=>{
'use strict';
const VERSION='GAMAGORI-TWO-STAGE-V0.26.4';
const todayJst=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Tokyo',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
const isCurrentLive=s=>!!s&&s.runType==='LIVE'&&s.venue==='蒲郡'&&String(s.date||'')===todayJst();
const safeEsc=v=>typeof window.esc==='function'?window.esc(String(v??'')):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function installSimpleUi(){let style=document.getElementById('bc-two-stage-simple-style');if(!style){style=document.createElement('style');style.id='bc-two-stage-simple-style';document.head.appendChild(style)}style.textContent=`#predictionList .race-card{padding:12px 14px!important}#predictionList .race-head{margin-bottom:9px!important}#predictionList .race-no{font-size:24px!important}#predictionList .two-stage-v0260{display:grid!important;grid-template-columns:1fr 1fr;gap:8px;margin:0!important}#predictionList .race-actions{margin-top:8px!important}.bc-stage-row{border:1px solid #24475f;background:#081a2a;border-radius:12px;padding:11px 12px;min-height:82px}.bc-stage-row.bc-first{border-color:#2b607d;background:#071a29}.bc-stage-row.bc-second.ready{border-color:#277b5d;background:#09241c}.bc-stage-row.bc-second.wait{opacity:.72}.bc-stage-top{display:flex;justify-content:space-between;align-items:center;gap:8px;margin-bottom:7px}.bc-stage-title{font-size:12px;font-weight:900}.bc-stage-tag{font-size:9px;color:#8db2cf;border:1px solid #31516e;border-radius:999px;padding:3px 7px}.bc-stage-picks{font-size:20px;font-weight:900;line-height:1.35;color:#eef9ff}.bc-stage-row.bc-second.ready .bc-stage-picks{color:#9dffd0}.bc-stage-status{font-size:12px;color:#8ca8bd;line-height:1.45;padding-top:6px}.bc-stage-detail{margin-top:7px}.bc-stage-detail summary{font-size:10px;color:#7796ae;cursor:pointer}@media(max-width:760px){#predictionList .two-stage-v0260{grid-template-columns:1fr}.bc-stage-picks{font-size:19px}}`}
function validResult(v){const x=String(v||'').trim();return /^[1-6]-[1-6]-[1-6]$/.test(x)&&new Set(x.split('-')).size===3?x:null}
function fallbackProgramClasses(r){const boats=r?.livePreRace?.racelist?.boats;if(!Array.isArray(boats)||boats.length!==6)return null;const sorted=[...boats].sort((a,b)=>Number(a.lane)-Number(b.lane));if(sorted.some((b,i)=>Number(b.lane)!==i+1))return null;return sorted.map(b=>String(b.class||'B1'))}
function currentComposition(s,r){let comp=null;try{comp=typeof window.analyzeProgram==='function'?window.analyzeProgram(s,r):null}catch{}if(comp?.available&&Array.isArray(comp.classes)&&comp.classes.length===6)return comp;const classes=fallbackProgramClasses(r);return classes?{available:true,classes,type:classes.join('-'),fingerprint:classes.join('|')}:null}
function firstCandidate(s,r){if(r?.firstSuggestion?.status==='CANDIDATE'&&r.firstSuggestion.lockedAt&&r.firstSuggestion.sessionDate===s.date)return r.firstSuggestion;const comp=currentComposition(s,r);if(!comp)return{status:'WAIT',reason:'当日番組データ待ち',sessionDate:s.date};const scores=new Map();let samples=0;const sessions=Object.values(window.store?.sessions||{});for(const hs of sessions){if(!hs?.date||String(hs.date)>=String(s.date))continue;for(const hr of hs.races||[]){if(!hr?.settled)continue;const res=validResult(hr.result);if(!res)continue;let hc=hr.programComposition||null;if(!hc){const classes=fallbackProgramClasses(hr);if(classes)hc={classes,type:classes.join('-'),fingerprint:classes.join('|')}}if(!hc?.classes||hc.classes.length!==6)continue;let sim=.15;if(hc.type===comp.type)sim+=1.2;for(let i=0;i<6;i++)if(hc.classes[i]===comp.classes[i])sim+=.32;if(hc.fingerprint===comp.fingerprint)sim+=2.5;scores.set(res,(scores.get(res)||0)+sim);samples++}}
if(!scores.size)return{status:'WAIT',reason:'過去番組データ蓄積待ち',sessionDate:s.date};const picks=[...scores].sort((a,b)=>b[1]-a[1]).slice(0,4).map(x=>x[0]),now=new Date().toISOString();return{status:'CANDIDATE',stage:'FIRST',picks,samples,sessionDate:s.date,generatedAt:now,lockedAt:now,strategyVersion:VERSION,rationale:`${s.date}の結果・展示は不使用。過去の類似番組${samples}件から照合。`}}
function refresh(){let s;try{s=window.session()}catch{return}if(!isCurrentLive(s))return;let changed=false;for(const r of s.races||[]){if(r.firstSuggestion?.sessionDate&&r.firstSuggestion.sessionDate!==s.date){delete r.firstSuggestion;changed=true}const x=firstCandidate(s,r);if(JSON.stringify(r.firstSuggestion||null)!==JSON.stringify(x)){r.firstSuggestion=x;changed=true}}if(changed&&typeof window.saveStore==='function')window.saveStore()}
const firstHtml=r=>{const x=r?.firstSuggestion;if(x?.status!=='CANDIDATE')return `<div class="bc-stage-row bc-first"><div class="bc-stage-top"><b>第一候補</b><span class="bc-stage-tag">過去データ</span></div><div class="bc-stage-status">${safeEsc(x?.reason||'準備中')}</div></div>`;return `<div class="bc-stage-row bc-first"><div class="bc-stage-top"><b>第一候補</b><span class="bc-stage-tag">過去データ 🔒</span></div><div class="bc-stage-picks">${x.picks.map(safeEsc).join(' / ')}</div><details class="bc-stage-detail"><summary>理由を見る</summary>${safeEsc(x.rationale)}</details></div>`};
const secondHtml=(s,r)=>{const x=r?.liveSuggestion;if(x?.sessionDate&&x.sessionDate!==s.date)return `<div class="bc-stage-row bc-second wait"><div class="bc-stage-top"><b>第二候補</b><span class="bc-stage-tag">展示後</span></div><div class="bc-stage-status">展示待ち</div></div>`;if(!x||x.status==='WAIT')return `<div class="bc-stage-row bc-second wait"><div class="bc-stage-top"><b>第二候補</b><span class="bc-stage-tag">展示後</span></div><div class="bc-stage-status">${safeEsc(x?.reason||'展示待ち')}</div></div>`;if(x.status==='SKIP')return `<div class="bc-stage-row bc-second wait"><div class="bc-stage-top"><b>第二候補</b><span class="bc-stage-tag">なし</span></div><div class="bc-stage-status">${safeEsc(x.reason||'直前情報不足')}</div></div>`;return `<div class="bc-stage-row bc-second ready"><div class="bc-stage-top"><b>第二候補</b><span class="bc-stage-tag">展示反映 🔒</span></div><div class="bc-stage-picks">${(x.picks||[]).map(safeEsc).join(' / ')}</div></div>`};
function render(){installSimpleUi();let s;try{s=window.session()}catch{return}if(!isCurrentLive(s))return;document.querySelectorAll('#predictionList .race-card').forEach(card=>{const race=Number((card.querySelector('.race-no')?.textContent||'').replace(/\D/g,'')),r=s.races.find(x=>Number(x.race)===race);if(!r)return;let box=card.querySelector('.two-stage-v0260');if(!box){box=document.createElement('div');box.className='two-stage-v0260';card.appendChild(box)}box.innerHTML=firstHtml(r)+secondHtml(s,r)})}
function sync(){refresh();render()}
const priorRender=typeof window.renderAll==='function'?window.renderAll:null;if(priorRender)window.renderAll=function(){const out=priorRender.apply(this,arguments);sync();return out};const priorSweep=typeof window.sweepVerifiedLiveRelays==='function'?window.sweepVerifiedLiveRelays:null;if(priorSweep)window.sweepVerifiedLiveRelays=async function(opts={}){const out=await priorSweep(opts);sync();return out};window.addEventListener('boatcommand:today-live',()=>{setTimeout(sync,0);setTimeout(()=>{if(typeof window.sweepVerifiedLiveRelays==='function')window.sweepVerifiedLiveRelays({render:true})},80)});window.BOAT_COMMAND_TWO_STAGE_V0260=Object.freeze({version:VERSION,venue:'蒲郡',currentDateOnly:true,firstUsesSameDayResults:false,firstUsesExhibition:false,noResultLookahead:true,sync});installSimpleUi();sync();
})();